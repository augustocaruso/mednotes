"""Setup workflow domain package.

`/mednotes:setup` is the recovery route for environment/configuration blockers.
The StateChart lives here as pure domain code; adapters execute the emitted
effects elsewhere.
"""

